%h1
  New bill

- form_for(@bill, :html => { :multipart => true }) do |f|
  = f.error_messages

  = render :partial => 'fields', :locals => {:f => f}

  %p
    = f.submit "Create"

= link_to 'Back', bills_path
