%h1
  Bills

= link_to 'New bill', new_bill_path, :class => 'bill_add'
%br

%h3 open
= render :partial => 'bill', :collection => @bills_open

%br

%h3 closed
= render :partial => 'bill', :collection => @bills_closed
