- if logged_in?
  #user-bar
    Logged in as
    %br
    %span.user
      = link_to_current_user :content_method => :login
    %br
    = link_to "&raquo; Log out", logout_path, { :title => "Log out" }
- else
  #user-bar
    Not logged in
    %br
    = link_to "Log in",  login_path,  { :title => "Log in" }
    |
    = link_to "Sign up", signup_path, { :title => "Create an account" }
